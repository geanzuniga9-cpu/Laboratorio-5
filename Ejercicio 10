using UnityEngine;

public class Multiplicadordedaño
{
    float daño = 100f;
    bool critico;
    bool vulnerable;

    public void Start()
    {
        float multiplicador = (critico && vulnerable) ? 2.5f : (critico || vulnerable) ? 1.5f : 1f;
        float dañoFinal = daño * multiplicador;
    }
}
